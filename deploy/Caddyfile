{
  email {$ACME_EMAIL}
  admin off
}

{$STAGING_DOMAIN} {
  encode zstd gzip
  @api path /api/* /health
  handle @api {
    reverse_proxy api:3001
  }
  handle {
    reverse_proxy web:3000
  }
  header {
    Strict-Transport-Security "max-age=31536000"
    X-Content-Type-Options nosniff
    Referrer-Policy strict-origin-when-cross-origin
  }
  # Deliberately no access log: request URLs can contain private signed URLs.
}
